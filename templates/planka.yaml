apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: planka 
spec:
  project: default
  source:
    repoURL: https://anthonymag.github.io/helm-charts/
    targetRevision: 1.2.1
    helm:
      valueFiles:
        - values.yaml
      parameters:
        - name: planka.image.tag
          value: v2.0.0
    chart: planka
  destination:
    server: https://kubernetes.default.svc
    namespace: planka
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace=true
  
